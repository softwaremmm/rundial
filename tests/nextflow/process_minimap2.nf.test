nextflow_process {
    name "Test Process minimap2"
    script "process/variant_calling.nf"
    process "minimap2"

    test("Process run successfully and produce alignment") {
        when {
            process{
                """
                input[0] = ['sample', file("$projectDir/test_data/assemblers/ont_simple.fastq.gz"), file("$projectDir/data/NC_000962.3.fa.gz")]
                """
            }
        }

        then {
            assert process.success
            assert process.out.sorted_alignment[0][1].contains('final.bam')
        }
    }
}
